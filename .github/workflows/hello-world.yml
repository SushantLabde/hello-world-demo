name: Hello World
on:
    push:
    workflow_dispatch:
    
jobs:
    say-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Print a greeting
              run: echo "Hello, World!"